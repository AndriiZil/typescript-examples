### For compiling file
```
    tsc app.ts
```
### initialize typescript
```
    tsc --init
```
### For excluding files from compiller add "exclude" to tsconfig.json or "include" or "files" for including files
for compiling.
### For watching files type
```
    tsc -w
    tsc --watch
```